"""The generic, definition-driven engine.

Nothing about any specific agent lives in code here — an agent is a versioned
:class:`~app.engine.definition.AgentDefinition` (a JSON document of blocks) that
this engine interprets at runtime.
"""
